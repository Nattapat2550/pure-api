# =========================
# Server
# =========================
NODE_ENV=development       # development | production
PORT=5000                  # เวลาอยู่บน Render มันจะ override เอง แต่อันนี้เผื่อรัน local


# =========================
# PostgreSQL
# =========================
# ตัวอย่างแบบ local:
# DATABASE_URL=postgres://user:password@localhost:5432/myapp_db
#
# ตัวอย่าง Render Postgres:
# DATABASE_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=postgres://user:password@host:5432/dbname


# =========================
# JWT
# =========================
JWT_SECRET=super-secret-jwt-key-change-this
JWT_EXPIRES_IN=30d          # ตอนนี้โค้ด fix 30d อยู่ ถ้าอยากผูกกับค่านี้ค่อยไปแก้ภายหลังได้


# =========================
# CORS / Frontend origins
# =========================
# ใส่ origin ของ frontend ที่ให้ยิงมาหา pure-api ได้ (comma-separated)
# dev:
#   React   -> http://localhost:3000
#   Angular -> http://localhost:4200
FRONTEND_URL=http://localhost:3000,http://localhost:4200


# =========================
# API Keys แยก client
# (pure-api ใช้สำหรับเช็ค x-api-key)
# =========================
API_KEY_ANDROID=android-key-123
API_KEY_WINDOWS=windows-key-123
API_KEY_REACT=react-key-123
API_KEY_ANGULAR=angular-key-123
API_KEY_DOCKER=docker-key-123


# =========================
# Download file paths (optional)
# =========================
# ถ้าในโค้ด download.service.ts คุณ fix path ไว้แล้ว อันนี้จะไม่ใช้ก็ได้
DOWNLOAD_WINDOWS_PATH=./app/MyAppSetup.exe
DOWNLOAD_ANDROID_PATH=./app/app-release.apk


# =========================
# Misc (optional)
# =========================
LOG_LEVEL=info
RATE_LIMIT_AUTH_MAX=30
